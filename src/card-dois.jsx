import React from 'react'
import Card from './card'
import './card-dois.scss'

const CardDois = ({children}) => {
  return (
    <Card className="card-dois">{children}</Card>
  )
}

export default CardDois
